#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    if(n == 1) {
        printf("1 is neither prime nor composite\n");
    } else {
        int a = 0; 
        for(int i = 2; i <= n/2; i++) {
            if(n % i == 0) {
                a = 1;  
                break;
            }
        }

        if(a == 0)
            printf("%d is a prime number\n", n);
        else
            printf("%d is a composite number\n", n);
    }

    return 0;
}
