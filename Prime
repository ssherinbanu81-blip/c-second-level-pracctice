if(n <= 1) {
    printf("%d is not a prime number\n", n);
} else {
    int a = 0; 
    for(int i = 2; i <= n/2; i++) {
        if(n % i == 0) {
            a = 1;    
            break;
        }
    }

    if(a == 0)
        printf("%d is a prime number\n", n);
    else
        printf("%d is not a prime number\n", n);
}
