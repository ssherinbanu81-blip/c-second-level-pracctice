#include <stdio.h>

int main() {
    int n, sum = 0, term = 0;
    printf("Enter number of terms: ");
    scanf("%d", &n);

    for(int i = 1; i <= n; i++);
        term = term * 10 + 1;  
        sum += term;           
    }

    printf("Sum of series = %d\n", sum);

    return 0;
}
