#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);          

    int f= 0, s= 1, t;

    for(int i = 1; i <= n; i++) {
        printf("%d ", f);     
        t = f + s;         
        f = s;                
        s = t;             
    }

    return 0;
}
